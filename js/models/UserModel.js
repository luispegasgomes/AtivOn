export default class UserModel {
  constructor(name, username, email, password, birthdate, gender) {
    this.name = name;
    this.username = username;
    this.email = email;
    this.password = password;
    this.birthdate = birthdate;
    this.gender = gender;
  }
}
